import BaseTitle from './UI/BaseTitle';

const Home = () => {
  return (
    <div>
      <BaseTitle title="Welcome to React, Redux, Tailwind CSS - Pokestore" />
    </div>
  );
};

export default Home;
